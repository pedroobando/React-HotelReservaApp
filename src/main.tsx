import React from 'react';
import ReactDOM from 'react-dom/client';
import { HotelReservApp } from './HotelReservApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HotelReservApp />
  </React.StrictMode>
);
